/// Categories
enum CategoriesGridListType{
  gridImageCardList,
  gridColoredCardList,
}

enum CategoriesVerticalListType{
  verticalImageCardList,
  verticalThumbnailList,
  verticallList,
}

enum CategoryPostsCountStyle{
  labelStyle,
  circleBadge,
  sequareBadge,
}


/// Posts
enum PostsListType{
  verticalList,
  cardStyleList,
  cardTypeTwoStyleList,
}

enum SortPostsOption{
  latest,
  oldest,
  mostComments,
  reset,
}